name: abr-hls-csharp

services:
  streaming-service:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      STREAMS_DIR: /app/streams
      SOURCE_DIR: /app/fixtures
    volumes:
      - hls_output:/app/streams

volumes:
  hls_output:
